import React from "react";
import { Center, Heading } from "@chakra-ui/react";

export default function Divisibility() {
  return (
    <Center flexDirection="column">
      <Heading>Divisibility Checker</Heading>
    </Center>
  );
}
